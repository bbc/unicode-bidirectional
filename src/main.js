import { resolvedLevelsWithInvisibles } from './resolve/resolvedLevels';
import { reorderedLevels } from './resolve/reorderedLevels';

// Public API
export {
  resolvedLevelsWithInvisibles as resolve,
  reorderedLevels as reorder
};
